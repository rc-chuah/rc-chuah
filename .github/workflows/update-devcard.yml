name: daily-devcard
on:
  # Run manually from the Actions tab
  workflow_dispatch:
  push:
    paths: # Run any time this file is modified
      - ".github/workflows/update-devcard.yml"
  schedule: # Execute every 24 hours
    - cron: "0 0 * * *"
jobs:
  devcard:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: devcard
        uses: dailydotdev/action-devcard@3.2.1
        with:
          user_id: ${{ secrets.USER_ID }}
          type: wide
          commit_message: "Update ${filename}"
